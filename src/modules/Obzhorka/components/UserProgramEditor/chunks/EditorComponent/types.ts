export enum SexType {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}

export enum ActivityType {
  LOW = 'LOW',
  LIGHT = 'LIGHT',
  MIDDLE = 'MIDDLE',
  HIGH = 'HIGH',
  EXTREME = 'EXTREME',
}

export enum GoalType {
  MASS_GAIN = 'MASS_GAIN',
  NORMAL = 'NORMAL',
  WEIGHT_LOSS = 'WEIGHT_LOSS',
}
